SUMMARY='Command-line translator using web services'
HOMEPAGE='https://www.soimort.org/translate-shell'

require github [ user=soimort ]

PLATFORMS='~amd64 ~x86'

DEPENDENCIES='
  build+run:
    app-shells/bash
    sys-apps/gawk
'
