SUMMARY='Command-line translator using web services'
HOMEPAGE='http://soimort.org/translate-shell'

require github [ user=soimort ]

LICENCES='public-domain'
PLATFORMS='~amd64 ~x86'

DEPENDENCIES='
  build+run:
    app-shells/bash
    sys-apps/gawk
'
