SUMMARY='a fuzzy text selector for files and anything else you need to select'
HOMEPAGE='https://github.com/garybernhardt/selecta'
SCM_REPOSITORY='git://github.com/garybernhardt/selecta'

require scm-git

PLATFORMS='~amd64 ~x86'

DEPENDENCIES='
  run:
    dev-lang/ruby
'

src_install() {
  dobin $PN
}
